import type { DialogueLine } from '../types/dialogue';

export const DIALOGUES: Record<string, DialogueLine[]> = {
  chapter1_intro: [
    {
      speakerId: 'narrator',
      textKey: 'dialogue.chapter1.intro.1',
    },
  ],
};
